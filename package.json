{
  "devDependencies": {
    "jest": "^29.7.0",
    "jsdom": "^26.0.0",
    "live-server": "^1.2.2"
  },
  "scripts": {
    "dev":"live-server",
    "test":"jest -watch",
    "coverage": "jest --coverage"
  },

  "jest": {
    "testPathIgnorePatterns": [
      "./__tests__/helpers.js"
    ]
  }
}
